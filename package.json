{
  "name": "gradient-image",
  "version": "1.0.0",
  "description": "A simple way to apply gradient filters to an image and save the result",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "gpiress",
  "license": "ISC",
  "dependencies": {
    "angular": "^1.6.2",
    "bootstrap": "^3.3.7",
    "jquery": "^3.1.1"
  }
}
